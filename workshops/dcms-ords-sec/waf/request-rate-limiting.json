{
  "rules": [
    {
      "actionName": "Pre-configured 401 Response Code Action",
      "condition": "i_contains(http.request.url.path, 'get')",
      "conditionLanguage": "JMESPATH",
      "configurations": [
        {
          "actionDurationInSeconds": 0,
          "periodInSeconds": 1,
          "requestsLimit": 20
        }
      ],
      "name": "DoS",
      "type": "REQUEST_RATE_LIMITING"
    }
  ]
}
