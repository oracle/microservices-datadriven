## Copyright (c) 2024, 2026, Oracle and/or its affiliates.
## Licensed under the Universal Permissive License v1.0 as shown at http://oss.oracle.com/licenses/upl.
# Example values for tenant2 installation
# This keeps optional unique ingress-nginx settings for multi-tenant opt-in deployments.
#
# Install with:
#   helm upgrade --install obaas-tenant2 . -n tenant2 -f examples/values-tenant2.yaml --create-namespace

# Optional legacy ingress controller configuration for this tenant
ingress-nginx:
  enabled: false
  controller:
    # Unique IngressClass name for this tenant
    ingressClass: nginx-tenant2
    ingressClassResource:
      # Must be unique across all tenants
      name: nginx-tenant2
      enabled: true
      default: false
      # Unique controller value - prevents conflicts with other tenants
      controllerValue: "k8s.io/ingress-nginx-tenant2"
    # Unique election ID for leader election
    electionID: "ingress-controller-leader-tenant2"
    # Scope to watch only this tenant's namespace
    scope:
      enabled: true

# Configure SigNoz admin credentials for this tenant
signoz:
  enabled: true
  auth:
    email: "admin-tenant2@example.com"
