## Copyright (c) 2024, 2026, Oracle and/or its affiliates.
## Licensed under the Universal Permissive License v1.0 as shown at http://oss.oracle.com/licenses/upl.

# =============================================================================
# Example: Private Registry Configuration for OBaaS Chart
# =============================================================================
#
# This example shows how to configure all images to pull from a private
# container registry (e.g., for air-gapped installations).
#
# IMPORTANT: This file is for the 'obaas' chart only. For the 'obaas-prereqs'
# chart, see the corresponding example in ../obaas-prereqs/examples/
#
# ----- Setup Steps -----
#
# Step 1: Create the image pull secret in your target namespace:
#
#   kubectl create secret docker-registry myregistry-secret \
#     --docker-server=myregistry.example.com \
#     --docker-username=<username> \
#     --docker-password=<password> \
#     --docker-email=<email> \
#     -n obaas
#
# Step 2: Mirror the required images to your private registry.
#   See the Image Reference Summary at the bottom of this file for the full list.
#
# Step 3: Set your secret name in global.imagePullSecretName below.
#   Replace "myregistry-secret" with your secret name — keep everything else as-is:
#
#   global:
#     imagePullSecretName: &secretName myregistry-secret   # <-- your secret name here
#     imagePullSecrets: &imagePullSecrets
#       - *secretName
#
#   IMPORTANT: Do not rename or remove &secretName, &imagePullSecrets, or *secretName.
#   These are YAML anchors that propagate the value to OBaaS templates and subcharts.
#
# Step 4: Install or upgrade the chart:
#
#   helm upgrade --install obaas . -n obaas \
#     -f examples/values-private-registry.yaml
#
# =============================================================================

nameOverride: ""
fullnameOverride: ""

# -----------------------
# -- Global Configuration
# -----------------------
global:
  # All OBaaS components install into the release namespace (specified with -n flag)
  # There is no global namespace override - components use the namespace you provide during install

  # When true, all PVCs in the namespace are deleted during helm uninstall
  # This ensures a clean reinstall but destroys all persistent data
  cleanupPVCs: true

  # When true, creates resources needed for air-gapped (disconnected) environments
  # including a self-signed TLS certificate for accounts.google.com
  airGapped: true

  # Image pull secret name — this is the ONLY value you need to change (see Step 3 above).
  # The &secretName anchor propagates it to OBaaS templates and subcharts.
  imagePullSecretName: &secretName myregistry-secret

  # Image pull secrets as a string list — used by OBaaS templates and subcharts that expect
  # ["secret-name"] format. OBaaS helpers normalize strings to {name: ...} objects.
  imagePullSecrets: &imagePullSecrets
    - *secretName

  # Utility images used by global hooks (e.g., PVC cleanup on uninstall)
  utilities:
    kubectl:
      image:
        registry: myregistry.example.com
        repository: alpine/k8s
        tag: "1.28.13"
    nginx:
      image:
        registry: myregistry.example.com
        repository: nginx
        tag: "alpine"

# =============================================================================
# OBaaS Components Configuration
# =============================================================================
# Note: Cluster-singleton prerequisites must be installed via the obaas-prereqs chart before installing this chart.
# Install obaas-prereqs once per cluster.
#
# Image Override Pattern:
# All image configurations are commented out by default to use upstream defaults.
# To use a private registry or air-gapped installation, uncomment and set the image values.
# Example: Change "registry.k8s.io" to "myregistry.example.com" for all components.


# AI Optimizer and Toolkit
ai-optimizer:
  enabled: false
  global:
    baseUrlPath: "/ai-optimizer"
  server:
    replicaCount: 1
    # -- Image details: Required for BYO Infra
    # imagePullSecrets: []
    # image:
    #   repository: localhost/ai-optimizer-server
    #   tag: latest
    #   pullPolicy: IfNotPresent
  client:
    enabled: true
    # -- Image details: Required for BYO Infra
    # imagePullSecrets: []
    # image:
    #   repository: localhost/ai-optimizer-client
    #   tag: latest
    #   pullPolicy: IfNotPresent

# Eureka Service Discovery
eureka:
  global:
    imagePullSecrets: *imagePullSecrets
  enabled: true
  replicas: 3
  # Keep Jetty server connections alive longer than APISIX/OpenResty's
  # 60-second pooled connection lifetime to avoid reusing server-closed sockets.
  connectionIdleTimeout: 90s
  image:
    repository: myregistry.example.com/obaas/service-registry
    tag: "2.1.0"
  # Override default resources if needed (defaults: cpu 250m-500m, memory 1Gi-1200Mi)
  resources: {}

# Admin Server
admin-server:
  global:
    imagePullSecrets: *imagePullSecrets
  enabled: true
  replicas: 1
  image:
    repository: myregistry.example.com/obaas/admin-server
    tag: "2.1.0"
  # Override default resources if needed (defaults: cpu 250m-500m, memory 256Mi-512Mi)
  # resources: {}

# Config Server
config-server:
  global:
    imagePullSecrets: *imagePullSecrets
  enabled: true
  replicas: 1
  image:
    repository: myregistry.example.com/obaas/config-server
    tag: "2.1.0"
  # Override default resources if needed (defaults: cpu 250m-500m, memory 256Mi-512Mi)
  # resources: {}

# Oracle Transaction Manager for Microservices (OTMM - MicroTX)
# Each component (coordinator, workflowServer, console) has its own image and
# can be toggled independently. The console only deploys when the coordinator
# and/or workflow server is also enabled.
otmm:
  global:
    imagePullSecrets: *imagePullSecrets
  coordinator:
    enabled: true
    replicas: 1
    image:
      repository: myregistry.example.com/database/microtx-coordinator
      tag: "26.1.1"
    # Override default resources if needed (defaults: cpu 250m, memory 1Gi)
    # resources: {}
  workflowServer:
    enabled: true
    replicas: 1
    image:
      repository: myregistry.example.com/database/microtx-workflow
      tag: "26.1.1"
  console:
    enabled: true
    image:
      repository: myregistry.example.com/database/microtx-console
      tag: "26.1.1"

# Oracle Database Exporter (Prometheus metrics for Oracle Database)
oracle-database-exporter:
  global:
    imagePullSecrets: *imagePullSecrets
  imagePullSecrets: *imagePullSecrets
  enabled: true
  replicas: 1
  image:
    repository: myregistry.example.com/database/observability-exporter
    tag: "2.4.2"
  # Busybox sidecar for tailing alert.log
  busybox:
    repository: myregistry.example.com/busybox
    tag: "1.36"
  # Override default resources if needed (defaults: cpu 250m-500m, memory 64Mi-128Mi)
  # resources: {}

# =============================================================================
# Database Configuration
# =============================================================================
# Supports SIDB-FREE, ADB-FREE, ADB-S (OCI Autonomous Database), or OTHER (external database)

database:
  global:
    imagePullSecrets: *imagePullSecrets
  # Database type: SIDB-FREE, ADB-FREE, ADB-S, or OTHER
  type: "SIDB-FREE"
  # SQLcl image for database initialization jobs
  sqlcl:
    image:
      repository: myregistry.example.com/database/sqlcl
      tag: "26.2.0"

  # For SIDB-FREE/ADB-FREE: container image configuration (exclude for ADB-S)
  image:
    repository: "myregistry.example.com/database/free"
    tag: "23.26.2.0"

  # For ADB-S: OCI Autonomous Database OCID (exclude for SIDB-FREE/ADB-FREE/OTHER)
  oci:
    ocid: ""

  # For OTHER: External database connectivity
  # Either provide 'dsn' OR all of (host, port, service_name)
  other:
    # Full DSN string (e.g: mydbhost.com:1521/mydb_service)
    # If provided, supersedes host/port/service_name
    dsn: ""
    # Database host (e.g: mydbhost.com)
    # Required if dsn is not provided
    host: ""
    # Database port (e.g: 1521)
    # Required if dsn is not provided
    port: ""
    # Database service name (e.g: mydb_service)
    # Required if dsn is not provided
    service_name: ""

  # Privileged User Authentication (for database initialization)
  # This is the ADMIN (ADB) or SYSTEM (non-ADB) user that creates the application user
  # ADB-S and OTHER: REQUIRED for these pre-created databases
  # SIDB-FREE, ADB-FREE: OPTIONAL; If not provided, a secret will be auto-generated with random password.
  privAuthN:
    # Name of Secret containing privileged user credentials
    # secretName: ""
    # Namespace of the privileged user secret
    # secretNamespace: ""
    # Override default key names in the secret (defaults: username, password, service)
    # usernameKey: "username"
    # passwordKey: "password"
    # serviceKey: "service"

  # Application User Authentication/Connection Details
  authN:
    # Namespace containing application user secret (secretName)
    # secretNamespace: ""
    # Name of Secret containing the authentication/connection details
    # If not provided, a secret will be auto-generated with random password
    # secretName: ""
    # Override default key names in the secret (defaults: username, password, service)
    # usernameKey: "username"
    # passwordKey: "password"
    # serviceKey: "service"

  # OCI Configuration (for ADB-S authentication)
  # Either use API key authentication OR Workload Identity
  oci_config:
    # For Workload Identity authentication:
    # Set to true to use OKE Workload Identity (no API key needed but requires Workload Identity)
    # oke: false

    # Namespace of the configmap/secret for OCI API Authentication
    # configNamespace: ""

    # For API key authentication:
    # Name of secret containing oci_api_key.pem
    # keySecretName: ""

    # Option 1: Reference to existing ConfigMap with OCI config
    # If provided, tenancy/user/fingerprint/region below will be ignored
    # configMapName: ""

    # Option 2: Provide OCI config values to create a ConfigMap
    # Only use these if configMapName is not provided
    # tenancy: ""
    # user: ""
    # fingerprint: ""
    # region: ""

# =============================================================================
# Subcharts - Envoy Gateway
# =============================================================================

gateway-helm:
  enabled: true
  config:
    envoyGateway:
      provider:
        type: Kubernetes
  global:
    imageRegistry: myregistry.example.com
    imagePullSecrets: *imagePullSecrets
    images:
      envoyGateway:
        image: "myregistry.example.com/envoyproxy/gateway:v1.8.2"
        pullPolicy: Always
        pullSecrets: *imagePullSecrets
      envoyProxy:
        image: "myregistry.example.com/envoyproxy/envoy:distroless-v1.38.3"
        pullPolicy: IfNotPresent
        pullSecrets: *imagePullSecrets
      ratelimit:
        image: "myregistry.example.com/envoyproxy/ratelimit:master"
        pullPolicy: IfNotPresent
        pullSecrets: *imagePullSecrets

# =============================================================================
# Subcharts - Ingress NGINX
# =============================================================================
# Default registry: registry.k8s.io

ingress-nginx:
  enabled: false
  # ingress-nginx requires [{name: ...}] format (upstream chart, cannot use string anchor)
  imagePullSecrets:
    - name: *secretName
  controller:
    metrics:
      enabled: true
    podAnnotations:
      "signoz.io/scrape": "true"
      "signoz.io/port": "10254"
      "signoz.io/path": "/metrics"
    # Scope to release namespace only (default is false - watches all namespaces)
    scope:
      enabled: true
    # Uncomment to override the default image registry
    image:
      registry: myregistry.example.com  # Default: registry.k8s.io
      image: ingress-nginx/controller
      tag: "v1.15.1"
      digest: "sha256:594ceea76b01c592858f803f9ff4d2cb40542cae2060410b2c95f75907d659e1"
    admissionWebhooks:
      patch:
        image:
          registry: myregistry.example.com
          image: ingress-nginx/kube-webhook-certgen
          tag: "v1.6.9"
          digest: "sha256:01038e7de14b78d702d2849c3aad72fd25903c4765af63cf16aa3398f5d5f2dd"


# =============================================================================
# Subcharts - APISIX
# =============================================================================
# Default registry: docker.io

apisix:
  enabled: true
  # Uses global.imagePullSecrets (expects string list format)
  global:
    imagePullSecrets: *imagePullSecrets
    security: 
      allowInsecureImages: true

  # RBAC required for Kubernetes service discovery
  rbac:
    create: true
  serviceAccount:
    create: true

  # APISIX configuration
  apisix:
    # Minimal plugins list - only what routes actually use
    # (prometheus is in APISIX defaults, but opentelemetry is not)
    plugins:
      # Required
      - api-breaker
      - authz-keycloak
      - basic-auth
      - batch-requests
      - consumer-restriction
      - opentelemetry
      - prometheus
      # Need Evaluation
      - ai
      - authz-casbin
      - authz-casdoor
      - aws-lambda
      - azure-functions
      - body-transformer
      - cas-auth
      - chaitin-waf
      - clickhouse-logger
      - client-control
      - cors
      - csrf
      - datadog
      - degraphql
      - echo
      - elasticsearch-logger
      - example-plugin
      - ext-plugin-post-req
      - ext-plugin-post-resp
      - ext-plugin-pre-req
      - fault-injection
      - file-logger
      - forward-auth
      - google-cloud-logging
      - grpc-transcode
      - grpc-web
      - gzip
      - hmac-auth
      - http-dubbo
      - http-logger
      - inspect
      - ip-restriction
      - jwe-decrypt
      - jwt-auth
      - kafka-logger
      - kafka-proxy
      - key-auth
      - ldap-auth
      - limit-conn
      - limit-count
      - limit-req
      - loggly
      - loki-logger
      - mocking
      - multi-auth
      - opa
      - openfunction
      - openid-connect
      - openwhisk
      - proxy-cache
      - proxy-control
      - proxy-mirror
      - proxy-rewrite
      - public-api
      - real-ip
      - redirect
      - referer-restriction
      - request-id
      - request-validation
      - response-rewrite
      - rocketmq-logger
      - server-info
      - serverless-post-function
      - serverless-pre-function
      - skywalking-logger
      - sls-logger
      - splunk-hec-logging
      - syslog
      - tcp-logger
      - tencent-cloud-cls
      - traffic-split
      - ua-restriction
      - udp-logger
      - uri-blocker
      - wolf-rbac
      - workflow
      - zipkin

    # Service Discovery
    discovery:
      enabled: true
      registry:
        # Eureka service discovery - connects directly to each StatefulSet replica
        # so APISIX can fail over within the same registry refresh cycle.
        eureka:
          host:
            - "http://{{ .Release.Name }}-eureka-0.{{ .Release.Name }}-eureka.{{ .Release.Namespace }}.svc.cluster.local:8761"
            - "http://{{ .Release.Name }}-eureka-1.{{ .Release.Name }}-eureka.{{ .Release.Namespace }}.svc.cluster.local:8761"
            - "http://{{ .Release.Name }}-eureka-2.{{ .Release.Name }}-eureka.{{ .Release.Namespace }}.svc.cluster.local:8761"
          prefix: "/eureka/"
          fetch_interval: 30
          timeout:
            connect: 2000
            send: 5000
            read: 15000
          weight: 100
        # Kubernetes service discovery
        kubernetes: {}

    # OpenTelemetry - connects to SigNoz via the signoz-otel-collector alias service
    # (see templates/observability/Service-signoz-otel-collector-alias.yaml)
    pluginAttrs:
      opentelemetry:
        collector:
          address: "http://signoz-otel-collector:4318"
          request_timeout: 3
        resource:
          service.name: APISIX
        trace_id_source: x-request-id
      prometheus:
        export_uri: /metrics
        metric_prefix: apisix_
        enable_export_server: true
        export_addr:
          ip: 0.0.0.0
          port: 9091

    # Environment variables required for Kubernetes service discovery
    nginx:
      envs:
        - KUBERNETES_SERVICE_HOST
        - KUBERNETES_SERVICE_PORT

  # etcd configuration
  etcd:
    enabled: true
    global:
      imagePullSecrets: *imagePullSecrets
    replicaCount: 3
    image:
      registry: myregistry.example.com
      repository: bitnamilegacy/etcd
      tag: "3.6.1"

  # APISIX image configuration
  # Full registry path required for clusters with short name mode enforcing
  image:
    repository: "myregistry.example.com/apache/apisix"
    tag: "3.17.0-ubuntu"

  # Init container image
  initContainer:
    image: "myregistry.example.com/busybox"
    tag: "1.36"


# =============================================================================
# Subcharts - SigNoz (Observability)
# =============================================================================
# SigNoz has many components. The global.imageRegistry covers most,
# but some components need explicit configuration.

signoz:
  enabled: true
  global:
    imageRegistry: myregistry.example.com
  # Utility images for setup jobs
  utilities:
    curlJq:
      image:
        registry: myregistry.example.com
        repository: curlimages/curl
        tag: "8.20.0"
    busybox:
      image:
        registry: myregistry.example.com
        repository: busybox
        tag: 1.36
  # Authentication configuration for SigNoz admin user
  auth:
    # Existing secret configuration
    # If existingSecret is specified, it will be used instead of creating a new one
    # The secret must contain 'email' and 'password' keys
    existingSecret: ""
    # Email for the admin user (only used when existingSecret is not specified)
    # A random password will be automatically generated
    email: "admin@example.com"
  # Change to override individual component images
  signoz:
    image:
      registry: myregistry.example.com
      repository: signoz/signoz
    imagePullSecrets: *imagePullSecrets
    initContainers:
      init:
        image:
          registry: myregistry.example.com
          repository: busybox
          tag: "1.36"
  schemaMigrator:
    image:
      registry: myregistry.example.com
      repository: signoz/signoz-schema-migrator
    initContainers:
      init:
        image:
          registry: myregistry.example.com
          repository: busybox
          tag: "1.36"
      chReady:
        image:
          registry: myregistry.example.com
          repository: clickhouse/clickhouse-server
      wait:
        image:
          registry: myregistry.example.com
          repository: groundnuty/k8s-wait-for
  telemetryStoreMigrator:
    enabled: true
  otelCollector:
    image:
      registry: myregistry.example.com
      repository: signoz/signoz-otel-collector
    # ClusterRole rules for Kubernetes service discovery (prometheus receiver)
    clusterRole:
      rules:
        - apiGroups: [""]
          resources: ["pods", "namespaces", "nodes", "services", "endpoints", "events"]
          verbs: ["get", "list", "watch"]
        - apiGroups: ["apps"]
          resources: ["replicasets"]
          verbs: ["get", "list", "watch"]
        - apiGroups: ["extensions"]
          resources: ["replicasets"]
          verbs: ["get", "list", "watch"]
        - apiGroups: ["batch"]
          resources: ["jobs"]
          verbs: ["get", "list", "watch"]
    config:
      receivers:
        # otlp:
        #   protocols:
        #     grpc:
        #     http:
        k8s_events:
          auth_type: serviceAccount
        prometheus:
          config:
            global:
              scrape_interval: 30s
              scrape_timeout: 10s
            scrape_configs:
              - job_name: kube-state-metrics
                kubernetes_sd_configs:
                  - role: endpoints
                relabel_configs:
                  - source_labels: [__meta_kubernetes_service_name]
                    action: keep
                    regex: .*kube-state-metrics
                  - source_labels: [__meta_kubernetes_endpoint_port_name]
                    action: keep
                    regex: http
              - job_name: apisix
                kubernetes_sd_configs:
                  - role: pod
                relabel_configs:
                  - source_labels:
                      [__meta_kubernetes_pod_label_app_kubernetes_io_name]
                    action: keep
                    regex: apisix
                  - source_labels: [__meta_kubernetes_pod_ip]
                    target_label: __address__
                    replacement: ${1}:9091
              # scrape all the pods with the signoz annotations on them
              - job_name: 'signoz-annotated-pods'
                kubernetes_sd_configs:
                  - role: pod
                relabel_configs:
                  - source_labels: [__meta_kubernetes_pod_annotation_signoz_io_scrape]
                    action: keep
                    regex: true
                  - source_labels: [__meta_kubernetes_pod_annotation_signoz_io_path]
                    action: replace
                    target_label: __metrics_path__
                    regex: (.+)
                  - source_labels: [__meta_kubernetes_pod_ip, __meta_kubernetes_pod_annotation_signoz_io_port]
                    action: replace
                    regex: ([^;]+);([^;]+)
                    replacement: $1:$2
                    target_label: __address__
                  - source_labels: [__meta_kubernetes_namespace]
                    action: replace
                    target_label: kubernetes_namespace
                  - source_labels: [__meta_kubernetes_pod_name]
                    action: replace
                    target_label: kubernetes_pod_name
                  - source_labels: [__meta_kubernetes_pod_label_app_kubernetes_io_instance]
                    action: replace
                    target_label: app                    
      service:
        pipelines:
          logs:
            receivers:
              - k8s_events
              - otlp
              - httplogreceiver/heroku
              - httplogreceiver/json
          metrics:
            receivers:
              - otlp
              - prometheus
  clickhouse:
    zookeeper:
      image:
        registry: myregistry.example.com
        repository: signoz/zookeeper
    image:
      registry: myregistry.example.com
      repository: clickhouse/clickhouse-server
    initContainers:
      udf:
        image:
          registry: myregistry.example.com
          repository: obaas/dashboards
          tag: "2.1.0"
        command:
          - sh
          - -c
          - |
            set -e
            node_os=$(uname -s | tr '[:upper:]' '[:lower:]')
            node_arch=$(uname -m | sed s/aarch64/arm64/ | sed s/x86_64/amd64/)
            echo "Node detected as ${node_os}/${node_arch}"
            src="/usr/local/bin/histogram-quantile/histogram-quantile_${node_os}_${node_arch}"
            dst="/var/lib/clickhouse/user_scripts/histogramQuantile"
            cp "$src" "$dst"
            chmod +x "$dst"
            echo "histogram-quantile installed successfully"
    clickhouseOperator:
      image:
        registry: myregistry.example.com
        repository: altinity/clickhouse-operator
      metricsExporter:
        image:
          registry: myregistry.example.com
          repository: altinity/metrics-exporter

k8s-infra:
  global:
    cloud: others
    clusterName: obaas
    deploymentEnvironment: obaas
    imageRegistry: myregistry.example.com
  otelCollectorEndpoint: signoz-otel-collector:4317
  otelInsecure: true
  otelAgent:
    image:
      registry: myregistry.example.com
      repository: otel/opentelemetry-collector-contrib
      tag: 0.139.0
  otelDeployment:
    image:
      registry: myregistry.example.com
      repository: otel/opentelemetry-collector-contrib
      tag: 0.139.0
  presets:
    otlpExporter:
      enabled: true
    loggingExporter:
      enabled: false

kafka:
  enabled: false
  version: 4.2.0
  metadataVersion: 4.2-IV1

# =============================================================================
# Image Reference Summary
# =============================================================================
#
# This table shows all images and their default registries:
#
# Component                          | Default Registry                      | Config Path
# -----------------------------------|---------------------------------------|------------------------------------------
# global kubectl (PVC cleanup hook)  | docker.io                     | global.utilities.kubectl.image.registry
# eureka                             | container-registry.oracle.com | eureka.image.repository
# admin-server                       | container-registry.oracle.com | admin-server.image.repository
# otmm coordinator                   | container-registry.oracle.com | otmm.coordinator.image.repository
# otmm workflow server               | container-registry.oracle.com | otmm.workflowServer.image.repository
# otmm console                       | container-registry.oracle.com | otmm.console.image.repository
# oracle-database-exporter           | container-registry.oracle.com | oracle-database-exporter.image.repository
# oracle-database-exporter (busybox) | docker.io                     | oracle-database-exporter.busybox.repository
# database                           | (varies by type)              | database.image.repository
# sqlcl                              | container-registry.oracle.com | database.sqlcl.image.repository
# ingress-nginx controller           | registry.k8s.io               | ingress-nginx.controller.image.registry
# ingress-nginx webhook              | registry.k8s.io               | ingress-nginx...admissionWebhooks...
# apisix                             | docker.io                     | apisix.image.repository
# apisix initContainer               | docker.io                     | apisix.initContainer.image
# apisix etcd                        | docker.io                     | apisix.etcd.image.registry
# signoz (all components)            | docker.io                     | signoz.global.imageRegistry + overrides
#
# =============================================================================
