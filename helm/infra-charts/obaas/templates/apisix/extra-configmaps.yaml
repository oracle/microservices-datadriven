{{- range $name, $data := .Values.extraConfigMaps }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $name }}
  namespace: {{ $.Release.Namespace }}
data:
  {{- range $key, $value := $data }}
  {{ $key }}: |
{{ $value | indent 4 }}
  {{- end }}
---
{{- end }}
