{{/*
Copyright (c) 2024, 2025, Oracle and/or its affiliates.
Licensed under the Universal Permissive License v1.0 as shown at http://oss.oracle.com/licenses/upl.
*/}}
Application {{ include "obaas-app.fullname" . }} deployed successfully!

Framework: {{ .Values.obaas.framework | upper }}
OBaaS Platform: {{ include "obaas.releaseName" . }}

{{- if .Values.ingress.enabled }}

Access your application via ingress:
{{- range $host := .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}
{{- end }}

{{- else if contains "NodePort" .Values.service.type }}

Get the application URL:
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "obaas-app.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT

{{- else if contains "LoadBalancer" .Values.service.type }}

Get the application URL:
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
  kubectl get --namespace {{ .Release.Namespace }} svc {{ include "obaas-app.fullname" . }} -w

{{- else if contains "ClusterIP" .Values.service.type }}

Access your application via port-forward:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "obaas-app.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
  echo "Visit http://127.0.0.1:{{ .Values.service.port }}"

{{- end }}

Configuration:
  - Eureka: {{ if .Values.eureka.enabled }}enabled{{ else }}disabled{{ end }}
  - OTEL: {{ if .Values.otel.enabled }}enabled{{ else }}disabled{{ end }}
  - OTMM: {{ if .Values.otmm.enabled }}enabled{{ else }}disabled{{ end }}
