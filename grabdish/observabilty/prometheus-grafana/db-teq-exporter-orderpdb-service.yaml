apiVersion: v1
kind: Service
metadata:
  name: db-teq-exporter-order
  labels:
    app: db-teq-exporter-order
    release: stable
spec:
  type: LoadBalancer
  ports:
    - port: 9161
      name: metrics
  selector:
    app: db-teq-exporter-order
